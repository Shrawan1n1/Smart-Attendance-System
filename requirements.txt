# ── requirements.txt — Smart Attendance System ────────────────────────────────
#
# Install all dependencies with:
#     pip install -r requirements.txt
#
# Then install the face recognition models separately:
#     python -m pip install git+https://github.com/ageitgey/face_recognition_models
#
# ⚠️  Python 3.10 is required. Python 3.12+ breaks dlib and face_recognition.
# ⚠️  numpy must stay below 2.0 — numpy 2.x is incompatible with dlib.
# ──────────────────────────────────────────────────────────────────────────────

# ── Face Recognition ──────────────────────────────────────────────────────────
cmake
dlib
face_recognition

# ── Image Processing ──────────────────────────────────────────────────────────
numpy<2
opencv-python
pillow

# ── Utilities ─────────────────────────────────────────────────────────────────
requests